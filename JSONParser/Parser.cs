using System.IO;

public partial class JSONParser {

    public class Parser {
        private TextReader reader;
        public Parser(TextReader reader) {
            this.reader = reader;
        }
    }

}
